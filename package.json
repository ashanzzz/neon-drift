{
  "name": "neon-drift",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "gh-pages": "^6.1.0",
    "terser": "^5.49.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
